# Local env for running the app locally (source this before `go run main.go`)
MONGO_INITDB_ROOT_USERNAME=development
MONGO_INITDB_ROOT_PASSWORD=development
MONGO_HOST=localhost
MONGO_PORT=27017
PORT=8000
# Optionally you can set MONGO_URI instead of the individual vars:
# MONGO_URI=mongodb://development:development@localhost:27017
